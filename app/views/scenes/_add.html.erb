<%= form_for(current_scene.relations.build(character_id: @scene.id)) do |f| %>
  <div><%= f.hidden_field :character_id %></div>
  <%= f.submit "ajouter", class: "btn btn-large btn-primary" %>
<% end %>